import { Mukta, Forum } from "next/font/google";

export const mukta = Mukta({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
});

export const forum = Forum({
  subsets: ["latin"],
  weight: "400",
});